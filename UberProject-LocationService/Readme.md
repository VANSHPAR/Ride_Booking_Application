### To start redis server
### Run below command in ubuntu

```bash
sudo service redis-server start
```

### To see status of redis server
### Run below command in ubuntu

```bash
sudo service redis-server status
```

### To stop redis server
### Run below command in ubuntu

```bash
sudo service redis-server stop
```